
1. Interfaces:
    Events:
        Configure nodes on insertion + test for it (KS)
        
    Methods:
     
2. Rendering:
    - Merge InternalMutationEvent with Interface notifications (CS)
    - Get rid of MeshManager (CS)
    - Cleanup CanvasHandler and Renderer as discussed (CS)
    - Make rendering work again (CS)
    
    Shading:
    Interaction:
    - Decide if we want to detect if picking is needed vs flag
    Xflow:
    
    
    