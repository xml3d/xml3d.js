The WebGL renderer is separated into several files. To build run the ant script 'build.xml' located in the 'merged' folder. From Eclipse IDE the build can also be started by running 'Run As->Ant build' from the files context menu.


